using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Else.HttpService.Helpers.CoreHelpers
{
    public static class CharArrayHelper
    {
        
        /*public static IEnumerable<Char> Append(this IEnumerable<char> Text1, String Text2)
        {
            return Append(Text1, Text2.ToCharArray());
        }

        public static IEnumerable<Char> Append(this IEnumerable<char> Text1, IEnumerable<char> Text2)
        {
            if (Text1 != null)
                foreach (var ch in Text1)
                    yield return ch;

            if (Text2 != null)
                foreach (var ch in Text2)
                    yield return ch;
        }*/
    }
}
